---
- name: Install linux apt packages
  ansible.builtin.apt:
    name: "{{ item }}"
    state: present
    update_cache: true
  loop: "{{ misc_packages_linux_apt_packages }}"
  become: true
  register: misc_packages_apt_packages
