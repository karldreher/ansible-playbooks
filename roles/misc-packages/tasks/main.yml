---
# tasks file for misc-packages

- name: Install misc packages
  package:
    name: "{{ packages }}"
    state: present
  vars:
    packages:
      - diceware
      - kubectx
      - pyenv
      - rectangle
      - rsync
      - tfswitch
      - tree
      - yq
      - zip
  tags:
    - packages
