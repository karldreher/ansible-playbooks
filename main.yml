---
- name: Configure local machine
  hosts: all
  roles:
    - role: git
    - role: misc_packages
    - role: gh_extensions
    - role: omz_zshrc
      when: ansible_facts["os_family"] == "Darwin"
    - role: misc_dotfiles
    - role: vim
    - role: claude
